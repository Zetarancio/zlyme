config BR2_PACKAGE_MERGERFS
	bool "mergerfs"
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  FUSE union mount. Pools internal, SD2, and USB ROM folders
	  into /storage/Roms.

comment "mergerfs needs a toolchain with C++"
	depends on !BR2_INSTALL_LIBSTDCPP
