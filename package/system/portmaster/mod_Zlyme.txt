#!/bin/bash
#
# SPDX-License-Identifier: MIT
#
# Sourced by every PortMaster port after control.txt:
#   [ -f "${controlfolder}/mod_${CFW_NAME}.txt" ] && source ...
#
# Platform bits only. HOME / XDG_* stay on the OS card.

PORT_32BIT="${PORT_32BIT:-N}"

GODOT2_OPTS="-r ${DISPLAY_WIDTH}x${DISPLAY_HEIGHT} -f"
GODOT_OPTS="--resolution ${DISPLAY_WIDTH}x${DISPLAY_HEIGHT} -f"

pm_platform_helper() {
	if [ -e "${PM_PIPE:-}" ]; then
		PortMasterDialogExit
	fi
	printf ""
}
