config BR2_PACKAGE_LIBRETRO_A5200
	bool "libretro-a5200"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-a5200 needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
