config BR2_PACKAGE_LIBRETRO_FREECHAF
	bool "libretro-freechaf"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-freechaf needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
