config BR2_PACKAGE_LIBRETRO_SAME_CDI
	bool "libretro-same-cdi"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-same-cdi needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
