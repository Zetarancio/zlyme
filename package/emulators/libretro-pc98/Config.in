config BR2_PACKAGE_LIBRETRO_PC98
	bool "libretro-pc98"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-pc98 needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
