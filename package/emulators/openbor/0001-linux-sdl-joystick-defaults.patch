--- a/engine/sdl/control.h	2024-01-01 07:28:27.000000000 +0100
+++ b/engine/sdl/control.h	2026-09-26 16:05:10.230487602 +0200
@@ -49,20 +49,25 @@
 	#define	CONTROL_DEFAULT1_ESC        (JOY_LIST_FIRST + 15)
 #else
 #ifdef SDL2
-	#define	CONTROL_ESC                 SDL_SCANCODE_ESCAPE
-	#define	CONTROL_DEFAULT1_START		SDL_SCANCODE_RETURN
-	#define	CONTROL_DEFAULT1_UP         SDL_SCANCODE_UP
-	#define	CONTROL_DEFAULT1_DOWN		SDL_SCANCODE_DOWN
-	#define	CONTROL_DEFAULT1_LEFT		SDL_SCANCODE_LEFT
-	#define	CONTROL_DEFAULT1_RIGHT		SDL_SCANCODE_RIGHT
-	#define	CONTROL_DEFAULT1_FIRE1		SDL_SCANCODE_A
-	#define	CONTROL_DEFAULT1_FIRE2		SDL_SCANCODE_S
-	#define	CONTROL_DEFAULT1_FIRE3		SDL_SCANCODE_Z
-	#define	CONTROL_DEFAULT1_FIRE4		SDL_SCANCODE_X
-	#define	CONTROL_DEFAULT1_FIRE5		SDL_SCANCODE_D
-	#define	CONTROL_DEFAULT1_FIRE6		SDL_SCANCODE_F
+	/* Linux player 1 used to be keyboard scancodes, so the opened
+	 * joystick never drove a character. These are joystick slots for
+	 * an xb360 pad with 15 buttons, 6 axes and hat 0: button 0 is A,
+	 * hat directions follow that button/axis count. JOY_LIST_FIRST
+	 * is 600. A saved per-pak file under Saves/ still overrides this. */
+	#define	CONTROL_ESC                 (JOY_LIST_FIRST + 1 + 6)
+	#define	CONTROL_DEFAULT1_START		(JOY_LIST_FIRST + 1 + 7)
+	#define	CONTROL_DEFAULT1_UP         (JOY_LIST_FIRST + 1 + 15 + 12)
+	#define	CONTROL_DEFAULT1_RIGHT		(JOY_LIST_FIRST + 1 + 15 + 12 + 1)
+	#define	CONTROL_DEFAULT1_DOWN		(JOY_LIST_FIRST + 1 + 15 + 12 + 2)
+	#define	CONTROL_DEFAULT1_LEFT		(JOY_LIST_FIRST + 1 + 15 + 12 + 3)
+	#define	CONTROL_DEFAULT1_FIRE1		(JOY_LIST_FIRST + 1 + 0)
+	#define	CONTROL_DEFAULT1_FIRE2		(JOY_LIST_FIRST + 1 + 3)
+	#define	CONTROL_DEFAULT1_FIRE3		(JOY_LIST_FIRST + 1 + 5)
+	#define	CONTROL_DEFAULT1_FIRE4		(JOY_LIST_FIRST + 1 + 4)
+	#define	CONTROL_DEFAULT1_FIRE5		(JOY_LIST_FIRST + 1 + 1)
+	#define	CONTROL_DEFAULT1_FIRE6		(JOY_LIST_FIRST + 1 + 2)
 	#define	CONTROL_DEFAULT1_SCREENSHOT	SDL_SCANCODE_F12
-	#define	CONTROL_DEFAULT1_ESC        SDL_SCANCODE_ESCAPE
+	#define	CONTROL_DEFAULT1_ESC        (JOY_LIST_FIRST + 1 + 6)
 #else
 	#define	CONTROL_ESC                 SDLK_ESCAPE
 	#define	CONTROL_DEFAULT1_START		SDLK_RETURN
