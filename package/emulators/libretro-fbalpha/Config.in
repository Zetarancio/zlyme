config BR2_PACKAGE_LIBRETRO_FBALPHA
	bool "libretro-fbalpha"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-fbalpha needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
