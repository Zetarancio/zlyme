config BR2_PACKAGE_LIBRETRO_TYRQUAKE
	bool "libretro-tyrquake"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-tyrquake needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
