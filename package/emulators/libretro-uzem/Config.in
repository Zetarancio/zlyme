config BR2_PACKAGE_LIBRETRO_UZEM
	bool "libretro-uzem"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-uzem needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
