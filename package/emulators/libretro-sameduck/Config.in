config BR2_PACKAGE_LIBRETRO_SAMEDUCK
	bool "libretro-sameduck"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-sameduck needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
