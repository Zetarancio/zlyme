config BR2_PACKAGE_LIBRETRO_THEODORE
	bool "libretro-theodore"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-theodore needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
