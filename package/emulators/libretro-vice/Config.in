config BR2_PACKAGE_LIBRETRO_VICE
	bool "libretro-vice"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-vice needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
