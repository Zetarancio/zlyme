config BR2_PACKAGE_LIBRETRO_EMUSCV
	bool "libretro-emuscv"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	select BR2_PACKAGE_SDL2
	help
	  Libretro core

comment "libretro-emuscv needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
