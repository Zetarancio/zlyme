config BR2_PACKAGE_LIBRETRO_PC88
	bool "libretro-pc88"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-pc88 needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
