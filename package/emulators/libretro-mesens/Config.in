config BR2_PACKAGE_LIBRETRO_MESENS
	bool "libretro-mesens"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-mesens needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
