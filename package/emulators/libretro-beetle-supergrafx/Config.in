config BR2_PACKAGE_LIBRETRO_BEETLE_SUPERGRAFX
	bool "libretro-beetle-supergrafx"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-beetle-supergrafx needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
