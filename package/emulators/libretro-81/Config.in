config BR2_PACKAGE_LIBRETRO_81
	bool "libretro-81"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-81 needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
