config BR2_PACKAGE_LIBRETRO_GENESISPLUSGX_WIDE
	bool "libretro-genesisplusgx-wide"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-genesisplusgx-wide needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
