config BR2_PACKAGE_LIBRETRO_WATARA
	bool "libretro-watara"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-watara needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
