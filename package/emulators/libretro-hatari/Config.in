config BR2_PACKAGE_LIBRETRO_HATARI
	bool "libretro-hatari"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	select BR2_PACKAGE_ZLIB
	help
	  Libretro core

comment "libretro-hatari needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
