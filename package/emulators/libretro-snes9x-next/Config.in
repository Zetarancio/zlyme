config BR2_PACKAGE_LIBRETRO_SNES9X_NEXT
	bool "libretro-snes9x-next"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-snes9x-next needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
