config BR2_PACKAGE_LIBRETRO_PCSX
	bool "libretro-pcsx"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-pcsx needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
