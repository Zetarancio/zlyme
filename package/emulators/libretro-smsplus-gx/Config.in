config BR2_PACKAGE_LIBRETRO_SMSPLUS_GX
	bool "libretro-smsplus-gx"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-smsplus-gx needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
