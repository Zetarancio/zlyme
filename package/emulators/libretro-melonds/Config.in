config BR2_PACKAGE_LIBRETRO_MELONDS
	bool "libretro-melonds"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	select BR2_PACKAGE_LIBPCAP
	help
	  Libretro core

comment "libretro-melonds needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
