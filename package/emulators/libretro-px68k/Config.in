config BR2_PACKAGE_LIBRETRO_PX68K
	bool "libretro-px68k"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-px68k needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
