config BR2_PACKAGE_LIBRETRO_FREEINTV
	bool "libretro-freeintv"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-freeintv needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
