config BR2_PACKAGE_LIBRETRO_TGBDUAL
	bool "libretro-tgbdual"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-tgbdual needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
