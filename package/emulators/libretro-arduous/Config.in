config BR2_PACKAGE_LIBRETRO_ARDUOUS
	bool "libretro-arduous"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-arduous needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
