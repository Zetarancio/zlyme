config BR2_PACKAGE_LIBRETRO_WASM4
	bool "libretro-wasm4"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-wasm4 needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
