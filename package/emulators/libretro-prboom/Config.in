config BR2_PACKAGE_LIBRETRO_PRBOOM
	bool "libretro-prboom"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Libretro core

comment "libretro-prboom needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
