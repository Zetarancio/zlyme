config BR2_PACKAGE_LIBRETRO_PICODRIVE
	bool "libretro-picodrive"
	depends on BR2_PACKAGE_RETROARCH
	depends on BR2_INSTALL_LIBSTDCPP
	select BR2_PACKAGE_LIBPNG
	help
	  Libretro core

comment "libretro-picodrive needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
