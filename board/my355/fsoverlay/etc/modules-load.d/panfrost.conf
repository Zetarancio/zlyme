# GPU kernel module is loaded by S15gpudriver from /storage/.config/zlyme/gpu
# (default libmali → mali_kbase). Do not autoload panfrost here: that would
# bind the GPU before the user choice is applied.
