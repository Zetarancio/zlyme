# Load btusb only after WiFi is up. Both halves share firmware;
# loading BT too early resets the chip and takes wlan0 down.
# Needs real kmod (busybox ignores blacklist and softdep).

blacklist btusb

softdep btusb pre: 8733bu
