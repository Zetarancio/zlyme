## Zlyme boot flags. The boot vfat is mounted at /boot (S12bootfs).

## Grow the 32MB ZLYME exFAT seed to fill the card, then format it.
## S13resize comments this out after it succeeds (Knulli autoresize).
## Re-enable only if you want that wipe-and-fill again.
autoresize=true
